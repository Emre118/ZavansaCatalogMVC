@model ProductCollection
@{
    ViewData["Title"] = "Koleksiyon Düzenle";
}

<h1 class="h3 mb-4">Koleksiyon Düzenle</h1>
<form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="card card-body">
    <partial name="_CollectionForm" model="Model" />
    <div class="mt-4 d-flex gap-2">
        <button class="btn btn-zavansa" type="submit">Güncelle</button>
        <a asp-action="Index" class="btn btn-outline-dark">Vazgeç</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
